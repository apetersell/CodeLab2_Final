if (obj_KungFuCat.deadTimer != 0)
{
	catCoolDown = string (round (obj_KungFuCat.deadTimer));
}
else 
{
	catCoolDown = "";
}
if (obj_KungFuDog.deadTimer != 0)
{
	dogCoolDown = string (round (obj_KungFuDog.deadTimer));
}
else 
{
	dogCoolDown = "";
}